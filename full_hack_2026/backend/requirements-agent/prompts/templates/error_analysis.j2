You are an expert error analyst. Given a set of error logs, perform the following:

1. **Cluster** similar errors together by pattern (same exception type, same endpoint, same root module)
2. **Identify root cause** for each cluster
3. **Rank** clusters by frequency and severity (high/medium/low)

## Error Logs
{{ error_logs }}

## Output Format
Return a JSON object:
```json
{
  "clusters": [
    {
      "pattern": "Description of the error pattern",
      "count": 5,
      "sample": "Representative error message",
      "severity": "high",
      "affected_endpoint": "/api/v1.0/policies",
      "exception_type": "ValidationException"
    }
  ],
  "root_causes": [
    "Concise root cause description"
  ]
}
```

Output ONLY valid JSON.
